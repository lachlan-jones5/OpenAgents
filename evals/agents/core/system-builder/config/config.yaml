# System Builder (Core Agent) Test Configuration
# Category-based path format

agent: core/system-builder
model: anthropic/claude-sonnet-4-5
timeout: 60000

# Test suite configuration
suites:
  - smoke
  - system-generation
